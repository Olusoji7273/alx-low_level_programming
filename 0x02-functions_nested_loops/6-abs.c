#include "main.h"
/**
 * _abs- Function that computes the absolute value of an integer
 * @n: This is the argument of the function
 * Return: It returns 0 after computing
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
		return (-n);
}
