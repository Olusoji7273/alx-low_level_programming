#include <stdio.h>
/**
 * main- Program that prints all single digit numbers of base 10 from 0
 *
 * Return: The code return 0 after execution
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);

	return (0);
}
