#include <stdio.h>
/**
 * main- A program that prints all digit numbers without using char
 *
 * Return: The program return 0 after execution
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		putchar(n);

	return (0);
}
