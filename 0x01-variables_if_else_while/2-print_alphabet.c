#include <stdio.h>
/**
 *main- Function that prints the alphabet in a lowercase
 *
 *Return: It return 0 in the block
 */

int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	putchar(ch);
	putchar('\n');

	return (0);
}
